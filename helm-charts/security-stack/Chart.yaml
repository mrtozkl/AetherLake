apiVersion: v2
name: security-stack
description: Centralized Identity and SSO via Keycloak for Data Lakehouse
type: application
version: 0.2.0
# Keycloak is now deployed from raw manifests using the upstream
# quay.io/keycloak/keycloak image and the official postgres image, with realm
# import handled by the upstream adorsys/keycloak-config-cli — no Bitnami charts.
